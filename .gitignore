# CMake build directories
build/
build-*/
out/

# Godot editor and import caches
demo/.godot/
demo/**/*.import
demo/**/*.uid

# Compiled GDExtension libraries (built from source, not checked in)
demo/**/*.so
demo/**/*.dll
demo/**/*.dylib

# Debug symbols (MSVC)
*.pdb

# IDE / editor
.idea/
.vscode/
*.swp
*~

# cargo mutants (legacy, keep for safety)
**/mutants.out*/
