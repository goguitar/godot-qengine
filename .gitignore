# CMake build directories
build/
build-*/
out/

# Godot editor and import caches
demo/.godot/
demo/**/*.import
demo/**/*.uid

# Compiled GDExtension libraries (built from source, not checked in by default)
demo/**/*.so
demo/**/*.dll
demo/**/*.dylib
# Exception: pre-built Windows x86_64 library committed for out-of-the-box testing
!demo/addons/qengine/bin/godot_qengine.dll
!demo/addons/qengine/bin/libgcc_s_seh-1.dll
!demo/addons/qengine/bin/libstdc++-6.dll

# Debug symbols (MSVC)
*.pdb

# IDE / editor
.idea/
.vscode/
*.swp
*~

# cargo mutants (legacy, keep for safety)
**/mutants.out*/
rust/target/
Cargo.lock
