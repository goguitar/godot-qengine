[package]
name        = "qengine-core"
description = "Guitar pitch detection logic: notes, tunings, SPSC ring buffer, multi-band detection via Q FFI"
version.workspace    = true
edition.workspace    = true
authors.workspace    = true
license.workspace    = true
repository.workspace = true

[dependencies]
qengine-sys = { path = "../qengine-sys" }
rtrb        = "0.3"

[dev-dependencies]
approx = "0.5"
